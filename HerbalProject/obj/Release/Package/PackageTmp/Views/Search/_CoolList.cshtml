@model List<herbDllProj.herbals>


@section Body{
    <div id="coolListWrapper">
        <span>Результаты поиска по запросу: "@ViewBag.searchedText" <span style="font-size: small;">(@Model.Count растений)</span> </span>

        @foreach (var item in @Model)
    {
            @Html.Partial("_CoolListItem", item);

    }
    </div>

}